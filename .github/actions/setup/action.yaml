name: Use secret PAT
description: Saves secrets.ACTIONS_PAT as env.GITHUB_TOKEN
runs:
  using: "composite"
  steps:
    - name: Use ACTION_PAT
      shell: bash
      run: |
        echo "GITHUB_TOKEN=$ACTIONS_PAT" >> "$GITHUB_ENV"
